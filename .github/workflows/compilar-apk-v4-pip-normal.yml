name: Compilar APK MasterPlus v4 PiP Normal

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Descomprimir proyecto v4
        run: |
          unzip MasterPlus_Android_v4_PiP_Normal_Proyecto.zip
          cp -r MasterPlus_Android_v4_PiP_Normal/* .
          cp -r MasterPlus_Android_v4_PiP_Normal/.github . 2>/dev/null || true

      - name: Configurar Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Configurar Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"

      - name: Compilar APK
        run: gradle assembleDebug

      - name: Guardar APK
        uses: actions/upload-artifact@v4
        with:
          name: MasterPlus-v4-PiP-Normal-APK
          path: app/build/outputs/apk/debug/app-debug.apk
